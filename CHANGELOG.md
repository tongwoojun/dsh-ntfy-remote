# 更新日志

## v1.1.0

**新增：回合心跳——手机上能看出 DSH 是在思考、卡住、还是已经断了。** 从手机发出一条消息后，
回合进行中会在话题里维护**一条**状态通知（`sequence_id` 原地更新，手机上始终只有一条，
不刷屏）：`🟢 思考中 · 1分20秒` → `🟠 疑似卡住`（响铃）→ `✅ 已完成 · 总时长`。
正文带「最近一次输出：12 秒前」，用来区分「秒数在走且真的在干活」与「秒数在走但一直没输出」。
状态通知上挂了「停止」按钮（点击发回 `/stop`，与手打完全等价——按钮文案和回执内容是分开的两个参数，中文只进 JSON body，不受 HTTP 头非 ASCII 的限制）。

关键在故障检测用的是 **ntfy 的 dead man's switch**，而不是客户端自己发心跳：回合开始时往
服务器挂一条 `3×心跳间隔` 的**定时告警**，每一拍把它往后推；进程一旦死掉就没人再推，
**服务器到点自己把 `🔴 DSH 心跳已停止` 发出来**——连「进程已退出」都能报，这是任何
"客户端自己发心跳"的方案做不到的。整个机制**不需要额外进程**：定时器跑在 DSH 进程里，
而"定时器停摆"本身（事件循环被阻塞）也正是我们要报的故障之一。

只暴露**一个**时间配置项「心跳间隔」（默认 20 秒，范围 5~300 秒），死信超时 = 3×、
卡住阈值 = 9× 由它推导，避免手填出「心跳 60 秒、死信 30 秒」这种健康时就会误报的组合。
子 agent 的回合不参与，避免一个父回合被子 agent 事件反复扰动。

**新增逐会话开关「网页发起的回合也推送」**（默认开，保持原有行为）：关掉后，只有
**手机发起**的回合才会通知手机——回合结束推送与状态心跳都停。适合"人就在电脑前，别再来
打扰我"的会话；代价是「在电脑上发起长任务、走开后手机收结果」这个场景也会一起没有，所以
做成开关而不是改默认。
区分依据是 `inflight`（手机发来的文本一律走 `executeTurn`，会标记它），不需要解析消息的
`source`——虽然它确实带 `rpcId` 可以事后区分网页发起的消息。
**注意默认方向**：判据是「没有显式关掉」而不是「显式打开」——这个键是后加的，任何缺少它
的旧配置都必须继续推送。（反过来写会让旧配置静默掐掉全部通知，集成测试当场抓到。）

**状态通知带上了 `dsh状态通知` 标签**，在手机上一眼能和普通推送区分开。它**不能替代**
内部那个 `dsh-ntfy-remote` 标记（回声过滤的主防线：插件订阅了自己发布的话题，服务器会把
消息原样推回来，靠标记才能认出是自己发的），所以两个 tag 并存；顺带把原来那两个只表达
状态的 emoji tag（`hourglass`/`white_check_mark`）去掉了——状态本来就在标题的 🟢/🟠/✅ 里。

**中止不再多推一条回执**（真机反馈）：点「停止」之后话题里原本会出现三条——你的
`/stop`、状态通知、以及 `/stop` 指令自己的回执「已请求中止当前回合。」。第三条是冗余的：
状态通知本来就会跟着回合结束变成结果。现在**回合因中止而结束时状态显示 `⏹ DSH 已中止 · N 秒`**
（而不是误导性的「✅ 已完成」），由它承担那句回执；心跳在跑时不再单独推确认。心跳不在跑
（例如对着空闲会话发 `/stop`）时仍会补那条确认，否则就没有任何反馈了。

**优雅停止时撤销死信开关**（真机踩出来的一条）：热重载或退出时 `stop()` 会清掉定时器，
但那条**挂在 ntfy 服务器上**的定时告警还在——不当场撤销的话，`3×心跳间隔` 后它会自己
投递，变成一条假的「🔴 心跳已停止」，而手机上会同时出现**两条**通知（🟢 状态 + 🔴 假告警，
因为两者的 `sequence_id` 不同）。现在 `stop()` 会逐个撤销。这个遗漏恰好**不伤真正的故障**：
进程崩溃时 `stop()` 根本不会执行，开关留在服务器上照样会响——所以「撤」这个动作本身
就区分了「优雅停止」与「崩溃」。

**多选提问现在也能点按钮了。** 之前 `multi_select` 一律不给按钮，只能回编号，原因是 ntfy 的
动作按钮是**无状态的单次回执**——点第二个只会多出一条互不相关的回执，表达不了「选中的集合」。
现在改成「**切换 + 提交**」两步：每个选项一个按钮，点一下切换该选项的选中状态（桥接侧在
`pending.selected` 里累积，不结算请求），最后一个「提交」按钮用累积结果结算。最后一组选项
最多放 2 个，腾出的按钮位留给「提交」；选项多于 3 个时照常拆成多条消息。
每次切换会回一条 `priority: 1` 的**静默**状态消息（`已选 2 项：甲项、丙项`）——ntfy 的按钮
无法回显选中态，没有它用户点完第二个就记不清第一个还在不在。直接回 `1,3` 的文本作答路径
仍然保留，想手打的人不会被按钮挡住。

**新增：撤回——在手机 App 里删掉自己发的消息，就能把那一轮收回来。** 在手机上发出去才发现
说错话，以前只能干等它跑完。现在在 ntfy 里删掉那条消息，插件收到服务器广播的 `message_delete`
后会对上号，**中止仍在跑的那一轮**，而且即使那一轮已经跑完，也**不再把回复推回手机**。

对号必须用删除事件里的 `sequence_id`，**不能用 `id`**。实测（ntfy 2.x，自建服务器）：消息发布时
带了 `sequence_id` 就是那个值；**没带（手机手打的普通消息都是这种）时，ntfy 会把被删消息自己的
id 填进 `sequence_id`**；而事件里的 `id` 是**删除事件自己**的新 id，与被删的那条无关——拿 `id`
对号会永远对不上。取消与 `/stop` 共用同一套静默窗口，所以不会跟着多推一条「回合中断：unknown」。

插件自己推出去的通知（状态通知、以及被撤销的死信开关）也会广播删除事件，那些对不上号，
按**常态**忽略并记一行日志——不是错误。顺带把「ntfy 消息 → 注入出来的 DSH 消息」这张表封了顶
（200 条，超出丢最旧的），否则常驻进程里它只增不减。

**修复：手机上的「空回复」被当成用户消息注入会话。** ntfy 安卓端在通知上点「回复」但一个字
都不输入时，话题里会收到一条正文正好是 `triggered` 的消息，而且它的字段与普通消息**完全一致**
（没有 tag、没有 title、priority 同样是 3），无法从元数据区分。旧实现把它当作用户输入注入会话，
顺手把正在跑的回合打断（真机实测：一条 `triggered` 让上一轮 4 秒后中止，还推了一条
「回合中断：unknown」）。现在按字面量认出并丢弃，**记日志而不是静默吞掉**；代价是你真想发
`triggered` 这个词会被吞掉——中文使用场景下可以接受。

**修复：过期的按钮回执被当成用户输入。** 每条通知上的按钮都带 `requestId`，但用户可能去点
**旧通知**上残留的按钮——那时请求早已结算或超时。旧实现会把整段 JSON 落到文本分支，原样注入
会话（模型收到一段乱码）。现在凡带 `requestId` 的结构化回执（`answer` / `approved` / `toggle` /
`submit` / `selected`），命中待决请求就结算，**没命中一律丢弃并记日志**，绝不落到文本分支。

**测试**：`probe/unit-check.mjs` 新增回合心跳断言（间隔钳制与空值回落、`死信=3×`/`卡住=9×`
的推导与「卡住阈值恒大于死信超时」「死信超时 ≥ ntfy 最小 delay」两条不变量、时长格式化、
四种状态的标题与正文、四种颜色互不相同）、多选按钮断言（每个选项都有切换按钮、恰好一个提交、
不夹带 `answer`、作答说明只出现一次）、`settlePending` 的切换 / 取消 / 提交，`isDecisionPayload` 的边界（`submit:false` 也算回执、
空 `requestId` 不算、纯文本不算），以及撤回（`handleDelete` 用 `sequence_id` 对号、没有
`sequence_id` 时退回 `id`、仍在跑才 cancel、与 `/stop` 共用静默窗口、对不上不抛错；
`pruneInjects` 封顶且丢最旧的一条）。
`probe/bridge-it.mjs` 新增 T8b2（多选点按钮切换 → 提交结算）、T21（心跳 14 项：ACK 响铃 /
`sequence_id` / `/stop` 按钮 / 原地更新两拍共用同一 seq / 收尾变 ✅ 且不再挂按钮）、T22（死信
开关 5 项：停掉定时器模拟进程卡死，**15 秒后服务器自行投递告警**，带冻结前的最后状态）、
T23（优雅停止撤销死信开关，不留假告警）、T24（中止时由状态通知吸收回执）、T25（关掉「网页发起
的回合也推送」后手机端完全静默）、T26（空回复哨兵不注入会话）、T27（**撤回**端到端：删掉自己
发的那条消息 → 那一轮被中止且回复不回推）。
`probe/routes-check.mjs` 改为以 `config.js` 的 `PREF_KEYS` 为唯一真源断言偏好键（以后新增键不会
误报）；`probe/client-check.mjs` 覆盖「网页发起的回合也推送」的界面说明。

## v1.0.6

**修复：分片推到手机后顺序错乱。** 分片之间原本背靠背发出，而 ntfy 的消息时间戳只精确到
**秒**、客户端按时间排序，同一秒内的多条会退化成一个不稳定的次序——真机实测三条分片在手机上
显示的先后是 1、3、2，用户读到的内容就是乱的。现在相邻两条之间拉开 1.1 秒（必然跨越秒边界），
并且改为**倒序发送**：ntfy 把最新的消息显示在最上面，倒着发才能让用户从上往下读到 1、2、3。
响铃仍挂在**内容第一条**（倒序后最后发出）上，等分片到齐再震一下。代价是一次分片推送多花几秒。

**修复：分片还没发完就点按钮，回执会丢。** 按钮挂在内容最后一块上，倒序发送时它恰好**最先**
送达，而待决请求原本要等 `sendMessages` 全部发完才登记。用户在剩余分片还在路上时点按钮，
回执匹配不到待决请求，会被当成普通消息注入会话，请求则继续空等到超时回落原生链（集成测试
稳定复现：只收到 1 条分片、答案是 null）。现在**先挂上待决请求再发送**；若一条都没发出去，
则撤销等待，不白挂一个到超时的请求。

**配置项 `maxMessageLength` 已移除。** 单条正文的字节预算改成固定值：ntfy 的硬上限是
4095 字节，超出的部分本来就会自动分片续发、内容不丢，所以「一条装多少」没有可调价值，
只会变成一个容易填错的坑——单位是字节而不是字，一个汉字占 3 字节，填 3500 看着像 3500 字，
实际只有 1165 个汉字。界面（设置页与状态页）与 `POST /config` 都不再接受它，旧配置里的
残留会在加载时自动清掉。

**测试**：`probe/bridge-it.mjs` 新增 T8e（分片未发完就作答的竞态），并断言相邻分片落在不同的
秒上、订阅流到达顺序为倒序（按标题 `(k/n)` 还原内容顺序后再断言）；
`probe/routes-check.mjs` 改为断言废弃字段不再落进配置、状态页不再残留「单条上限」控件。

## v1.0.5

**界面**：会话右上角 `● ntfy` 弹窗里新增**二维码**——「扫码添加到手机话题」。二维码内容就是
该会话的 `ntfy://服务器/话题` 深链接，用手机 ntfy App 扫一下即可订阅，不必手抄那串
`dsh_<会话 id>`。没开过桥接（没有话题）或绑定的服务器已被删除时不画码——那种情况下扫出来
也没有可订阅的东西。

**实现**：二维码在**宿主侧**现算，新增 `GET /dsh-ntfy-remote/session/qr?sessionId=…`，返回一张
自包含 SVG；编码器是新文件 `qr.js`（字节模式、纠错等级 M、版本 1–10 自动选、8 个掩码按
惩罚分自动挑）。刻意做成只认 `sessionId`、不认任意文本的端点：话题名不必发给任何第三方在线
生成服务，也不给「把任意内容渲染成二维码」留口子。客户端只摆一个 `<img>`，编码器全仓只有一份、
可以单测。

**测试**：`probe/unit-check.mjs` 新增 QR 段（版本边界 14/15/…/213/214、掩码、v1 与 v2 的**金标准
整矩阵**、v7 整矩阵摘要、定位 / 定时 / 暗模块结构不变量、SVG 结构与「不把深链接写进正文」）；
`probe/routes-check.mjs` 新增二维码路由 7 项（含 Content-Type、缺参 / 未绑定 → 404、响应体不含
明文深链接、与直接算出的深链接逐字节一致）；`probe/client-check.mjs` 新增弹窗二维码 7 项（图存在、
src 带 sessionId、alt、像素尺寸、可见标题与说明、未开启桥接不画码）。编码器开发期与
`qrcode@1.5.4` **逐位对齐过版本 1–10 × 8 个掩码共 152 组**，并用 jsQR 真解码过自动掩码的输出。

**修复：提问推到手机时内容大面积缺失。** `handleAskUserQuestion` 组装正文时只取了
`question.question` 和 `option.label` 两个字段，其余全丢：`option.description`（选项的取舍说明）、
`question.header`（短标题）、以及 `question.detail`——计划模式下 `dsh-plan-mode` 把**整份计划正文**
塞在这个字段里，桌面端会铺开显示，而手机上只收到一句「批准这个计划吗」，等于拿着空白批准。
现在按桌面端（`dsh-client-ui-user-questions`）的渲染口径补齐全部字段，并标注单选 / 多选与
「第几问 / 共几问」；顺带修掉一个旧漏洞：有按钮时回编号会落成 custom 而不是映射成标签。

**修复：长中文通知被 ntfy 静默拒收。** ntfy 的 `message` 字段上限是 **4095 字节**（实测 4096
字节返回 HTTP 500），而旧实现按**字符**截断到 3500——中文一个字 3 字节，折算 10500 字节，
服务端直接以 413「JSON body too large」拒收。`publish()` 失败只写一行日志、调用方又是
`void this.notify(...)`，于是**任何超过约 1365 个字的中文回合回复整条丢失**，用户什么都收不到。
现在改为按**字节**分片：单条预算默认 4000 字节（留余量给代码围栏修复），超出自动拆成多条，
内容不丢。切点优先落在行边界，硬切时按码点迭代累计字节，绝不劈开多字节字符（汉字 3 字节、
emoji 4 字节）。切点落在 ``` 代码块内部时，给上一片补闭合围栏、下一片重新打开围栏，
否则 md 渲染会从切点开始烂掉。`maxMessageLength` 的单位随之从「字」改为「字节」。

**分片的送达姿态**：一次提问拆成多条时，**只有第一条响铃**（priority 4），后续片一律
priority 1（min，静音）并在标题带 `(2/3)` 序号——让手机连震 4 次比少几条更烦。某一条失败
不中断整批，已送达的按钮仍能结算同一个 `requestId`；只要有一条成功就认为这批可用。
回声兜底的正文名单 `recentOwn` 上限从 3 提到 12，否则分片穿回去会被当成用户输入。

**markdown 渲染**：`publish()` 现在带 `markdown: true`，客户端会把标题、列表、代码块、
链接渲染出来。实测三种方式均生效（`X-Markdown: yes` 请求头、JSON 字段 `markdown: true`、
`Content-Type: text/markdown`），服务端在发布响应里回显 `content_type: text/markdown` 作为确认；
不带任何标记的对照组没有该字段。安卓端确认可渲染——`ntfy-android` 依赖 `markwon:core:4.6.2`
（含 `ext-tables` 表格扩展）。

**修复：自建服务器的动作按钮点下去 HTTP 403。** ntfy 的动作按钮是**手机直接**向服务器
发 POST，不走插件的 HTTP 客户端，所以服务器开了鉴权时 token 必须写进动作定义本身。
旧实现只写了 `label / url / method / body`，`headers` 是空的——在自建（`tokenSet=true`）
的服务器上点 `Approve` / `Deny` / 任意选项按钮一律被鉴权拒掉，用户只能退回手打编号作答。
现在按 ntfy 的 `http` 动作规范补上 `headers.Authorization = Bearer <token>`（无 token 时
不写该字段）。真机复现与验证：修复前点按钮得到 403，修复后话题里出现结构化的
`{"requestId":…,"answer":…}` 回执。按钮构造收口到 `buildActionButtons()`，可离线单测。

**测试**：`probe/unit-check.mjs` 新增 49 项——按字节分片（中文 / emoji / 超长单行 / 跨片代码
围栏开闭配对 / 预算给到 4095 也不越界 / 拼接无损）、提问组装（`description`、`header`、`detail`
全带上、编号跨组连续、每组按钮不超 3、多选不给按钮、多选 + 超 3 选项时描述仍然齐全）与
`buildActionButtons`（带 token 写 `Authorization`、无 token 不凭空造头）。
`probe/bridge-it.mjs` 新增 T8c（单选 4 选项）与 T8d（**多选** 4 选项，钉住用户报的「描述
不全」）两组端到端共 19 项，真发到 ntfy 并断言每条 `content_type` 为 `text/markdown`、
只有第一条 priority 4、后续条 priority 1、标题带 `(k/n)`、回编号能命中跨组选项、
多选时每个选项的标签与描述都在。
`probe/routes-check.mjs` 新增 4 项：状态页的**内联脚本不得引用宿主侧常量**——那段
`<script>` 是原样发给浏览器的，拼进 Node 模块作用域的标识符会在 `render()` 里抛
`ReferenceError` 而路由仍返回 200，只看状态码的测试根本发现不了（本次实际踩到并修复）。

## v1.0.3

**文档**：README「为什么用它」表格补上两行（多会话分流、按项目 / 环境分渠道），并在表格
之后新增「和微信 / 飞书通知的区别」小节，讲清「不绑人、绑会话」的思路——每个会话一个话题、
可配多个 ntfy 服务器、渠道与 Agent 解耦。纯文档改动，插件行为不变。

## v1.0.2

**界面**：右上角 `● ntfy` 弹窗最下面（「作答超时」之下）加了 **ntfy App 下载地址**：`iOS`
（App Store）与 `Android`（官方 GitHub Release 的 play 版 APK 直链）两个小按钮，样式与
「网页打开」一致，完整地址放在悬停 `title` 里。

## v1.0.1

**修复：多选提问只回一个选项。** DSH 的 `ask_user_question` 支持 `multi_select`，但桥接层
从未读过这个字段，且 ntfy 动作按钮是无状态的单次回执（点第二个只会多出一条回执，被当成
普通消息注入）。现在 `multi_select` 一律不下发按钮，改为编号列表作答，`1,3` 这样的回复会
映射成**多个** `selected` 标签（标签原文、`、`、空格同样可用），提示语也改成多选专用。

**修复：手机 `/stop` 会多推一条「回合中断：unknown」。** 手机发起的取消产生的是**没有嵌套
原因**的 `aborted`，`isUserInitiatedCancel()` 只认 `user / parent / disposed`，认不出来就落到
错误分支，把缺失原因渲染成 `unknown`。现在 `/stop` 会记一个 60 秒静默窗口，压掉随之而来的
那一条中断推送；窗口过期即丢弃，**不会**吞掉之后真正的异常中断（集成测试 T19 两个方向都断言）。

**修复：`/session/prefs` 不校验取值。** 之前 `relayTimeoutSec` 会照收 `0` / `-100` / `"abc"` /
`true`：界面显示「0 秒」而实际按 180 秒算（数字框清空后失焦提交的就是 `0`，正常操作就能踩到），
布尔键也会被塞进字符串。现在按 key 校验 —— 布尔键只收布尔，超时只收 ≥5 的有限数字，非法一律
`400 bad-value`；并且**与全局默认一致的赋值等同于恢复默认**，不再往 `overrides` 里堆没有意义
的项（输入框失焦即提交，不改数字也会写一条覆盖）。

**新增：会话归档后自动断开桥接。** DSH 的归档（本体 `workspaceRegistry.archivedSessionIds`）
**不删文件**，所以原来那条「不在持久化列表」的判定永远抓不到它 —— 归档过的会话会一直留在插件
列表里、继续订阅。现在 `sweep` 会读归档集合，命中就**立刻**清理（归档是明确的用户动作、持久
状态，不像「列表里找不到」那样可能是瞬时问题，因此不等三次核对）；真删除仍保持连续三次核对。
读不到归档集合时不动任何绑定。

**新增：会话列表每行的「删除」。** 设置 → Ntfy Remote 与独立状态页**只列出有记录的会话**（开过
桥接：有绑定 / 开关 / 偏好），每行的**删除**只删本插件里这条记录，**不碰 DSH 会话**（文件与历史
都在，之后重新 `/ntfy on` 即可）；与「解绑」不同，它不要求先关闭桥接，随时可用。活着但从没开过
桥接的会话不再列出来 —— 那种行没有可删的东西，放个按钮只会让人以为点了没反应；它们在自己的
`● ntfy` 弹窗里开启。对应 `POST /dsh-ntfy-remote/session/forget`，`/status` 每行新增 `recorded`
字段供界面过滤。

**测试**：`probe/bridge-it.mjs` 新增 T8b（多选编号列表 → 多个 `selected`）、T19（`/stop`
静默 + 真实中断仍推送）与 T20（四个开关逐项隔离：关掉各自不推送或回落原生链，恢复后照常）；
新增 `probe/routes-check.mjs`（离线路由自测：状态页 / 状态 JSON、参数校验 10 个反例、偏好
写入与清除、服务器保护、全局默认边界）。

**界面**：设置页名称统一为大写 **Ntfy Remote**（页面标题、无服务器时的提示、独立状态页
`<h1>` 同步）；会话里的「本会话通知偏好」原来只有一个孤零零的数字框（默认 180），现在
标注为**作答超时**并附说明：它是审批 / 提问推到手机后等待手机回复的秒数，超时即回落
DSH 原生交互——本地弹窗继续等，请求不会丢。说明在头部弹窗、设置页会话卡、全局默认与
独立状态页四处共用同一句文案，鼠标悬停数字框也能看到。话题行在「打开」旁边新增**复制**
按钮，点一下直接复制话题名（弹窗、设置页会话卡、独立状态页三处一致），成功后就地回显
「已复制」；剪贴板 API 在局域网明文 http 下不可用，退回 `execCommand` 兜底。「本会话通知
偏好」的四项也补上了**可见说明**（控件下方一行灰字，同时是悬停 `title`），缩写
「回合 / 待决 / 错误 / 手机优先」改成「回合结束推送 / 审批 · 提问推送 / 错误 · 中断推送 /
手机优先接管作答」。话题行去掉了**「手机跳转 ntfy://…」**——通知本来就落在话题里、点开即
可回复，这行只是噪音；`/status` JSON 仍保留 `deepLink` 字段，供外部脚本使用。弹窗底部那段
「手机 ntfy App 订阅这个话题接收推送；点通知即落在本话题……」的长提示也一并去掉。话题行
原来的蓝色文字链接「打开」改成**「网页打开」**，并和「复制」共用同一套小胶囊按钮样式
（`<a>` 与 `<button>` 外观一致），完整 URL 放在悬停 `title` 里；独立状态页同步。

**通知标题 / 会话名**：`labelFor()` 中间那段从工作目录名改成**该会话绑定的 ntfy 服务器名**，
形如 `DSH · 官方 ntfy.sh · cd1cc32e`——同一目录下开的多个会话以前标题完全一样，根本分不清；
未绑定的会话仍退回目录名。状态页会话列表用的是同一个标签，一并变化。

## v1.0.0

首个版本。把每个 DSH 会话桥接到一个 ntfy 话题。

**单话题**：每个会话一个话题（`dsh_<完整会话 id>`），出站推送与入站回复共用；点通知即
落在该话题、可直接打字回复，不依赖 `ntfy://` 深链接，iOS 也能用。插件靠消息里的
`dsh-ntfy-remote` 标签（外加 message id 与正文回声两道兜底）过滤自己发出的通知。
**推送**：回合结束的最终回复、待审批 / 待提问（高优先级）、错误与中断的精简原因。
**回复**：手机在会话话题里打字即可注入正在运行的会话；审批可点按钮或回编号。
**多服务器**：可配置多个带名称的 ntfy 服务器（官方或自建），会话首次开启时绑定，
之后不可变更；服务器失效时可解绑重选。
**逐会话偏好**：回合 / 待决 / 错误 / 手机优先 / 作答超时，均可按会话覆盖全局默认。
**界面**：会话头部 `● ntfy` 按钮（就地选服务器、开关、改偏好）；设置 → Ntfy Remote
（服务器增删改、全局默认偏好、所有会话总览）；以及独立状态页 `/dsh-ntfy-remote`。
**生命周期**：会话被删除后自动断开桥接，不留僵尸订阅。
**安装**：`dsh plugin --profile <p> add github:tongwoojun/dsh-ntfy-remote`（GitHub 直装，
装完自动加入 profile 的 `dsh.profile.bundles`，无需手改补丁）、`dsh plugin … add
dsh-ntfy-remote`（npm）、或本地路径。无构建步骤，包已可直接 `npm publish`。
**数据**：`$DSH_HOME/dsh-ntfy-remote/{config.json,state.json,plugin.log}`，JSON 原子写入。
